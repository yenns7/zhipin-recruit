# Zhipin MVP Deployment Handoff

This is the company server deployment package for the Zhipin MVP.

See `部署交接说明.md` for the Chinese handoff note.

Quick notes:

- Frontend build: `frontend/dist/`
- Backend DB: `backend/hireinsight.db`
- Backend deps: `backend/requirements.txt`
- Env template: `backend/.env.example`
- Create writable upload folder on server: `backend/uploads/`
- Proxy `/api` to the backend service
- Demo password: `Zhipin2026`
