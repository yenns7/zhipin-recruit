# 智聘·招聘管理系统 后端依赖
# Python 3.9+

# ── Web 框架 ─────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.5
Flask-SQLAlchemy==3.1.1
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0
MarkupSafe==3.0.3
click==8.4.1
gunicorn==26.0.0

# ── 数据库 / ORM ──────────────────────────────────────────
SQLAlchemy==2.0.50
# 生产 PostgreSQL 驱动（SQLAlchemy URL 使用 postgresql+psycopg://）
psycopg[binary]==3.3.4

# ── 认证 ─────────────────────────────────────────────────
PyJWT==2.13.0
cryptography==48.0.1
bcrypt==5.0.0

# ── 环境配置 ──────────────────────────────────────────────
python-dotenv==1.2.2

# ── AI / LangGraph ───────────────────────────────────────
langgraph==1.2.5
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langchain-core==1.4.7
langchain-protocol==0.0.17
# langchain-core 的间接依赖；显式锁定安全版本，避免旧版 langsmith 被解析进生产环境。
langsmith==0.8.18

# ── 简历解析 ──────────────────────────────────────────────
pypdf==6.13.3
pdfplumber==0.11.9
pdfminer.six==20251230
pypdfium2==5.9.0
python-docx==1.2.0

# ── 数据处理 ──────────────────────────────────────────────
pandas==3.0.3
requests==2.34.2
requests-toolbelt==1.0.0

# ── BOSS 直聘 CLI 集成（招聘端搜候选人/岗位管理/简历下载）─────
# 注意：招聘端 recruiter 子命令仅在 GitHub 源码版含，PyPI 发布包 kabi-boss-cli 未收录，
# 故必须从 GitHub 源码安装（boss-cli @ jackwener）。需 Python ≥3.10。
kabi-boss-cli @ git+https://github.com/jackwener/boss-cli.git

# ── 任务队列（开发用 eager 模式，生产可换 Redis）────────────
celery==5.6.3
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0

# ── 测试 ─────────────────────────────────────────────────
pytest-flask==1.3.0
